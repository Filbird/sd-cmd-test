shared:
  image: docker-registry.ops.yahoo.com:4443/ylinux/ylinux7:7-current

jobs:
  validate:
    requires: [~pr, ~commit]
    steps:
      - validate: sd-cmd validate -f ./sd-command.yaml

  publish:
    requires: [validate]
    steps:
      - publish: sd-cmd publish -f ./sd-command.yaml
      
